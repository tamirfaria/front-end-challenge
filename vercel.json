{
  "version": 2,
  "builds": [
    {
      "src": "./package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "parcel build ./src/index.html"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./src/index.html"
    }
  ]
}
